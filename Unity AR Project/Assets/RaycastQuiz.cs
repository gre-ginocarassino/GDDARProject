/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RaycastQuiz : MonoBehaviour
{
    private Camera _camera;
   [SerializeField] private MoveInParabola moveInParabola;
    void Awake()
    {
        _camera = Camera.main;
     
    }

    // Update is called once per frame
    void Update()
    {

        /*
        if (Input.GetMouseButtonDown(0))
        {
            Ray toMouse = _camera.ScreenPointToRay(Input.mousePosition);
            if(Physics.Raycast(toMouse, out var hit,100))
            {
                
                if (hit.transform.gameObject.GetComponent<MoveInParabola>().CompareTag("QuizTrigger"))
                {
                    
                    MoveInParabola parabola = hit.transform.gameObject.GetComponent<MoveInParabola>();
                    parabola.MoveObjectRoutineWrapper();
                    Debug.Log(hit.transform.tag);
                }
            }#1#
            
               
                
           
          
        }

        
    }
    
   
}
*/
